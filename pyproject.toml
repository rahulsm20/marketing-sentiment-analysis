[project]
name = "market-sentience"
version = "0.1.0"
description = "config for all python microservices in market sentience"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[tool.uv.workspace]
members = [
    "./generation-service",
    "./embedding-service",
]
